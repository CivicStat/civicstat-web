import PageShell from "../../components/PageShell";

export default function ApiPage() {
  return (
    <PageShell
      title="Open API"
      subtitle="Read-only endpoints voor moties, stemmingen en profielen."
    >
      <p>API-documentatie komt hier, inclusief rate limits en dataformaten.</p>
    </PageShell>
  );
}
